
application/config/constants.php

application/logs/

bulkload_stage_files/

testing/
.env

vendor/
composer.lock

application/config/keys/

# .htaccess real de cada dev (contiene URL de ngrok + paths locales).
# Usar .htaccess.example como plantilla. Nunca commitear el real.
.htaccess

# Backups de CAR generados automáticamente por development/repack-toolsapi-car.sh
development/*.car.bak.*
